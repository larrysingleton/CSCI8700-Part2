<% provide(:title, @user.name) %>
<h1>
  <%= gravatar_for @user %>
  <%= @user.name %>
  <p>Email: <%= @user.email %></p>
  <p>Default Bid:<%= @user.defaultBid %> </p>
</h1>

<%= link_to 'Edit', edit_user_path(@user), :class => "btn btn-default" %>